// Generated by CoffeeScript 1.6.3
(function() {
  var scaffolding;

  scaffolding = {
    setup: function() {
      var current_selected, numberOfSteps, show_step, stepsWidth, widths;
      console.log('Loading scaffolding');
      numberOfSteps = $("#steps > .step").length || 0;
      stepsWidth = 0;
      widths = [];
      $('.app .page').each(function(i) {
        var $step;
        $step = $(this);
        widths[i] = stepsWidth;
        return stepsWidth += $step.width();
      });
      current_selected = 0;
      return show_step = function(stepIndex) {
        current_selected = stepIndex % numberOfSteps;
        return $('.app').stop().animate({
          marginLeft: '-' + widths[current_selected] + 'px'
        }, 500);
      };
    },
    show_page: function(pageIndex) {
      $('.app .page').hide();
      return $($('.app .page').get(pageIndex)).show();
    },
    bindEvents: function() {
      console.log("Binding events for anchors.");
      return $('[data-show]').click(function() {
        var page_nr;
        page_nr = $(this).data('show');
        scaffolding.show_page(page_nr);
        return false;
      });
    }
  };

  window.scaffolding = scaffolding;

}).call(this);
